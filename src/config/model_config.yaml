# ================================
# Model Configuration (Google AI Studio)
# ================================

default_provider: DeepSeek-V3.2

# --------------------------------
# Agent → Model routing
# --------------------------------
# Assigned based on task complexity vs. speed requirements
routing:
  scope_agent: deepseek-chat
  supervisor_agent: deepseek-chat
  research_agent: deepseek-chat
  final_reporter: deepseek-reasoner
  summarizer: deepseek-reasoner

models:
  deepseek-chat:
    model: deepseek-chat
    temperature: 0.0
    max_tokens: 8000
    timeout: 15

  deepseek-reasoner:
    model: deepseek-reasoner
    temperature: 0.2
    max_tokens: 32000
    timeout: 30